{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "ptpBuild",
        "type": "shell",
        "command": "make",
        "options": {
          "cwd": "${workspaceFolder}"
        },
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
}